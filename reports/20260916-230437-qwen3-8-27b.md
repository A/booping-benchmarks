---
benchmark: frontmatter-update-e2e
model: qwen3.8-27b
run_id: 20260916-230437
date: 2026-09-16 23:04
outcome: pass
detail: ../runs/20260916-230437-qwen3-8-27b.md
---

# qwen3.8-27b — one agent, whole plan, thinking medium (run 2 of 3)

| harness | outcome | code | agentic | review | diff | time | tokens | cost |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| pi `direct`, one invocation for all three milestones, `--thinking medium` | pass, 1 attempt | 93.8 | 80.0 | 3.25 | 1539 | 14m28s | in 3.1M out 48.8k | $0.00 |

Every figure comes from the [run detail](../runs/20260916-230437-qwen3-8-27b.md); nothing is recomputed. Second of three same-setup runs taken on 2026-09-16 for run-to-run statistics.

## The task

The fixed develop-sprint benchmark: take the groomed three-milestone plan that migrates `booping frontmatter-update`'s unit suite into a txtar e2e corpus, implement it on a throwaway clone at baseline `a025189`, and leave a branch a runner can score — corpus written and green, the superseded unit file deleted after a coverage cross-check, `just ci` green end to end.

## Sprint narrative

Same setup as run 1, opposite worker temperament: this time pi honoured the contract to the letter — no commits, no bookkeeping, work left in the working tree for the runner to validate, split into per-milestone commits and close. One invocation, one attempt, 14m28s — the fastest of the three runs. M01 delivered 16 cases, M02 twelve more plus the real-execution macro case, and M03's cross-check actually caught two unit behaviours no milestone had covered and added cases for them (`sets-an-empty-value`, `default-vault-resolves-under-the-sandbox-home`) before deleting the unit file. The model also recorded rather than fixed a ruamel list-indent normalisation it noticed, per the plan's bugs-are-recorded rule. The flip side of the speed: the corpus is the smallest of the three (30 cases), and it shows in coverage.

## Gates

All six pass: `just ci` exit 0, determinism clean, `just e2e` green, milestones terminal, 36 changed paths inside the allowlist, unit file gone with zero stale references.

## Corpus quality

Exact-name recall 7/34, lifted to 29/34 by the case-mapping judge — the lowest mapped recall of the three runs. Five behaviours unported: the diff header naming the plan, a malformed `--append` pair, a dedicated partly-numeric-stays-string case, a dedicated plain-literal-set case, and a value carrying `=` signs. Gaps 2/3 (the malformed `--append` pair again). Four-channel rigour is the weak layer: 23/30 cases assert all four channels, costing 2.3 points of `code` — the only run of the three to drop any. Wildcard discipline clean. One branch case maps to no etalon (`default-vault-resolves-under-the-sandbox-home`), a defensible extra born of the cross-check.

## Mutation kills

15/15 (100%), same clean sweep as run 1 — every coercion, parsing, macro, remove, diff and stderr mutant caught. The kill set again never probes the unported seams.

## Process profile

One plan-scoped attempt, 89 tool calls (bash 34, write 31, read 21, edit 3), zero malformed inputs, loops, deaths and freezes. Agentic 80.0, all of it one line: **two blind rebaselines** — `--txtar-update` runs not preceded by a red e2e run — cost the full 20-point rebaseline weight. Churn is healthy (504 pushed vs 1539 diff).

## Review

- **fable:medium — 4.0** (correctness 4, tests 3, code 4, scope 5)
- **codex — 2.5** (correctness 2, tests 2, code 3, scope 3)

Mean 3.25, the lowest of the three runs, and the reviewers converge hard on one theme: the cross-check certifies coverage the corpus does not have. fable's worst finding: three cross-check rows map the `@`-prefix passthrough units to a case that never sets an `@`-value — the proof artifact M03 exists to produce is false on exactly the rows its DoD forbids; of the unit's eight syntax-sensitive quoting triggers only `a: b` survives. codex presses the same seam from the plan side: `test_value_with_equals_sign` was dropped though only hook-tokenising and live-git removals were plan-approved. Both name the missing malformed-`--append` case and the unpinned diff-header contract. The pattern in one sentence: a fast, clean sprint whose deletion step certified more than it verified.

## Standing

| date | att | code | agentic | review | time | tokens |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-16 22:42 (run 1) | 1 | 96.6 | 100.0 | 4.13 | 16m17s | in 3.5M out 50.9k |
| **this run** | **1** | **93.8** | **80.0** | **3.25** | **14m28s** | **in 3.1M out 48.8k** |
| 2026-09-13 17:57 | 1 | 96.7 | 75.0 | 4.25 | 15m15s | in 2.7M out 47.9k |
| 2026-09-13 11:45 | 1 | 96.3 | 100.0 | 4.38 | 19m24s | in 3.7M out 58.2k |

The weakest of the day's three same-setup runs on every judged layer — code (four-channel drop), agentic (blind rebaselines), review (false cross-check rows) — while being the fastest and cheapest. Against the model's wider one-agent band (code 95.0–97.2) it sits just below the floor; the spread between this run and run 1 is exactly the run-to-run variance the three-run series exists to expose.

## Footnotes

- Local provider: no OpenRouter generation ids; cost from the worker's own usage accounting.
- Scored with `bench-score`'s `--plan-segment M01-M02-M03`: one plan-scoped attempt, `att` is a single value.
- The worker left the tree uncommitted per contract; the `pi-developer:` milestone commits and the `develop:` close are the runner's bookkeeping split.
- Branch `bench/qwen3-8-27b` lives in workspace `.benchmarks/20260916-230312-qwen3-8-27b`; log `~/.tmp/pi-developer/20260916-230437-qwen3-8-27b.ndjson`.
