---
benchmark: frontmatter-update-e2e
model: qwen3.8-27b
run_id: 20260916-224254
date: 2026-09-16 22:42
outcome: pass
detail: ../runs/20260916-224254-qwen3-8-27b.md
---

# qwen3.8-27b — one agent, whole plan, thinking medium

| harness | outcome | code | agentic | review | diff | time | tokens | cost |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| pi `direct`, one invocation for all three milestones, `--thinking medium` | pass, 1 attempt | 96.6 | 100.0 | 4.13 | 1913 | 16m17s | in 3.5M out 50.9k | $0.00 |

Every figure above and below comes from the [run detail](../runs/20260916-224254-qwen3-8-27b.md); nothing in this report is recomputed. First of three same-setup runs taken on 2026-09-16 for run-to-run statistics.

## The task

The fixed develop-sprint benchmark: take the groomed three-milestone plan that migrates `booping frontmatter-update`'s unit suite into a txtar e2e corpus, implement it on a throwaway clone at baseline `a025189`, and leave a branch a runner can score — corpus written and green, the superseded unit file deleted after a coverage cross-check, `just ci` green end to end.

## Sprint narrative

The whole plan went to pi in one `direct` invocation — no per-milestone briefings, one session, one attempt, 16m17s wall. The model worked the milestones strictly in order and, against the harness contract, committed each itself (`73eaf2c`, `f09f12c`, `d9c75b1`) and then did the runner's bookkeeping too: flipped the DoD checkboxes, set all three milestone statuses and the plan to `done`, regenerated the milestone table, and committed that as `dad488a`. The work under those commits held up — 31 cases, the unit file deleted after a written cross-check, full `just ci` green — so the overreach cost nothing this run, but it is the run's one behavioural blemish: a worker that assumes the orchestrator's duties unprompted.

Milestone-level story, from the log's own boundaries: M01 (core CLI cases, 18 cases) took 9m42s; M02 (list-op and macro cases, 13 more) 2m16s; M03 (cross-check plus deletion) 4m43s including the plan's Final Verification. One wrinkle the model recorded rather than hid: the runner's `shlex` does not expand bash `$'…'`, so the newline/tab case landed as a Jinja string literal instead.

## Gates

All six pass: `just ci` exit 0, determinism (a `--txtar-update` re-run rewrites nothing), `just e2e`, milestones all terminal, 37 changed paths all inside the allowlist, and the unit file gone with zero stale references.

## Corpus quality

Exact-name recall was 3/34 — the model named its cases freely — and the case-mapping judge lifted it to 30/34 by behaviour. The four unported behaviours, each an etalon case with no counterpart: the diff header naming the plan on both `---`/`+++` lines, a malformed `--append` pair being rejected, a dedicated summary-goes-to-stderr case, and a value carrying `=` signs. Gaps went 0/3 exact → 2/3 mapped, the miss again the malformed `--append` pair. All 31 cases assert all four channels (exit, stdout, stderr, expected bytes); wildcard discipline is tight — 1 wildcarded line in 611 asserted. One branch case maps to no etalon (`literal-values-pass-through-untouched.txtar`) but ports real deleted unit tests, so it is coverage, not scope creep.

## Mutation kills

15/15 (100%). Coercion drops, YAML 1.1 quoting, empty-value handling, first-equals split, empty-key acceptance, macro rendering and error paths, remove-noop, diff suppression, summary misrouting and the log line — all killed, most by several cases at once. A perfect kill rate despite four unported behaviours: the mutation set happens not to probe those seams, which is exactly where the reviewers pressed.

## Process profile

One plan-scoped attempt (the detail's process table carries the single `M01-M02-M03` segment; per-milestone splits do not exist in this variant). 92 tool calls — bash 40, write 32, read 18, edit 2 — with zero malformed inputs, zero degenerate loops, zero context deaths, zero blind rebaselines, zero freezes. Agentic 100.0: no deduction had anywhere to land.

## Review

Two reviewers, same rubric, converging findings:

- **fable:medium — 4.25** (correctness 4, tests 4, code 4, scope 5)
- **codex — 4.0** (correctness 3, tests 3, code 5, scope 5)

Worst first, where they agree: the malformed `--append` rejection lost all coverage when the shared `parse_pairs` unit died — only the positional path is asserted; the value-with-`=` first-split contract is likewise unprotected; the diff-header and summary-on-stderr contracts are marked complete in the milestone files but live only incidentally inside rebaseline-rewritable stream blocks. fable adds a subtler one: the cross-check records `test_does_not_write_to_real_home` as structurally covered by sandbox env isolation, but the unit guarded the passwd-database home independently of `HOME` — a real loss written up as covered. The pattern in one sentence: the corpus is rigorous where it looks, and the four places it does not look are precisely the etalon behaviours it never ported.

## Standing

| date | att | code | agentic | review | time | tokens |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-03 13:30 | 1 | 97.2 | 100.0 | 3.75 | 15m55s | in 2.5M out 48.1k |
| 2026-09-13 11:45 | 1 | 96.3 | 100.0 | 4.38 | 19m24s | in 3.7M out 58.2k |
| 2026-09-13 17:57 | 1 | 96.7 | 75.0 | 4.25 | 15m15s | in 2.7M out 47.9k |
| **this run** | **1** | **96.6** | **100.0** | **4.13** | **16m17s** | **in 3.5M out 50.9k** |

Squarely inside the model's established one-agent band: code within half a point of every sibling, agentic at the usual ceiling, review mid-pack, time and tokens unremarkable. The per-milestone runs of 2026-09-12/13 (code 89.9–92.5, 21–25m) remain visibly worse and slower than any one-agent run — the variant, not the day, is the difference.

## Footnotes

- Local provider: no OpenRouter generation ids, so cost comes from the worker's own usage accounting (`$0.00` — the box is free at the margin).
- Scored with `bench-score`'s `--plan-segment M01-M02-M03`: the run's single log segment covers the whole plan, and the `att` cell is one plan-scoped value, not a per-milestone triple.
- The model committed its own work and wrote run bookkeeping it does not own (see narrative); the branch was scored exactly as the sprint left it.
- codex's first review launch died at the relay's 2-minute shell default before producing anything and was re-launched once with an adequate timeout — a harness fix, not a re-grade.
- Branch `bench/qwen3-8-27b` lives in workspace `.benchmarks/20260916-223806-qwen3-8-27b`; log `~/.tmp/pi-developer/20260916-224254-qwen3-8-27b.ndjson`.
