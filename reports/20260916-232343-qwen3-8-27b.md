---
benchmark: frontmatter-update-e2e
model: qwen3.8-27b
run_id: 20260916-232343
date: 2026-09-16 23:23
outcome: pass
detail: ../runs/20260916-232343-qwen3-8-27b.md
---

# qwen3.8-27b — one agent, whole plan, thinking medium (run 3 of 3)

| harness | outcome | code | agentic | review | diff | time | tokens | cost |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| pi `direct`, one invocation for all three milestones, `--thinking medium` | pass, 1 attempt | 96.3 | 90.0 | 3.63 | 1727 | 18m24s | in 3.0M out 54.4k | $0.00 |

Every figure comes from the [run detail](../runs/20260916-232343-qwen3-8-27b.md); nothing is recomputed. Third of three same-setup runs taken on 2026-09-16 for run-to-run statistics.

## The task

The fixed develop-sprint benchmark: take the groomed three-milestone plan that migrates `booping frontmatter-update`'s unit suite into a txtar e2e corpus, implement it on a throwaway clone at baseline `a025189`, and leave a branch a runner can score — corpus written and green, the superseded unit file deleted after a coverage cross-check, `just ci` green end to end.

## Sprint narrative

Contract-faithful like run 2: one `direct` invocation, one attempt, 18m24s, no commits, tree left for the runner. The largest corpus of the series — 34 cases, 19 in M01 and 15 in M02 — including the only run to port the value-keeps-equals-signs contract and to fold all eight syntax-sensitive quoting families into one case. M03 wrote a 54-row cross-check and deleted the unit file via `git rm`; the staged deletion then slipped into the runner's M01 commit by a staging accident — a bookkeeping blemish of the runner's, not the model's. The model also reasoned in writing about YAML 1.1 timestamp syntax (space-separated date-times are not timestamps), which is exactly the reasoning the reviewers later turned against its own date-like case.

## Gates

All six pass: `just ci` exit 0, determinism clean, `just e2e` green, milestones terminal, changed paths inside the allowlist, unit file gone with zero stale references.

## Corpus quality

Exact-name recall 8/34 — the best exact score of the three — lifted to 30/34 by the judge, tying run 1. Four behaviours unported: the diff mechanics as a subject, a dedicated empty-value case, protective quoting of genuine timestamp forms (the stub value `1970-01-01 12:00` is not a YAML 1.1 timestamp, so the "stays a string" case asserts a value never at risk), and the malformed `--append` pair. Gaps 2/3. Four-channel 33/34; wildcard ratio nominally clean, but see the review — one case wildcards its entire value. Four branch cases map to no etalon, two of them defensible extras (append-only nothing-to-change, unchanged-value no-diff) and one a verbatim duplicate.

## Mutation kills

15/15 (100%) — the third clean sweep of the series. The frozen set never probes the unported seams, which is where every reviewer point landed instead.

## Process profile

One plan-scoped attempt, 92 tool calls (bash 34, write 35, read 18, edit 5), zero malformed inputs, loops, deaths and freezes. Agentic 90.0: one blind rebaseline — a `--txtar-update` run not preceded by a red e2e — cost 10 of the rebaseline weight. Churn healthy (623 pushed vs 1727 diff).

## Review

- **fable:medium — 4.0** (correctness 4, tests 3, code 4, scope 5)
- **codex — 3.25** (correctness 3, tests 3, code 4, scope 3)

Mean 3.63. The worst finding is shared: `real-date-macro-executes-without-a-stub` wildcards its whole value — it passes on any output, proving neither live execution nor the string claim in its own description — and the cross-check maps six unit rows onto that non-assertion. Both reviewers name the not-actually-a-timestamp stub, the missing malformed-`--append` case, and the empty-value contract surviving only incidentally. fable adds the duplicate case with the false description and the three `@`-prefix cross-check rows mapped to a case with no `@`-value. The pattern in one sentence: the series' biggest corpus, undermined at its edges by assertions that look like coverage but do not constrain the behaviour they name.

## Standing

The full three-run series, same setup end to end:

| run | att | code | agentic | review | diff | time | tokens |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 — 22:42 | 1 | 96.6 | 100.0 | 4.13 | 1913 | 16m17s | in 3.5M out 50.9k |
| 2 — 23:04 | 1 | 93.8 | 80.0 | 3.25 | 1539 | 14m28s | in 3.1M out 48.8k |
| 3 — this run | 1 | 96.3 | 90.0 | 3.63 | 1727 | 18m24s | in 3.0M out 54.4k |

Series read: code is stable (93.8–96.6, mean 95.6), agentic swings a full 20 points on blind-rebaseline luck alone, and review spans 3.25–4.13 with the mutation kill rate pinned at 100% throughout — the judged layers, not the mechanical ones, carry all the run-to-run variance. Every run beats the per-milestone runs of 2026-09-12/13 (code 89.9–92.5, 21–25m) on both score and wall clock. Against the model's earlier one-agent rows (code 95.0–97.2, agentic 75–100) the series sits squarely in band; nothing about today's runs moves the model's standing, but three same-day points now bound its variance.

## Footnotes

- Local provider: no OpenRouter generation ids; cost from the worker's own usage accounting.
- Scored with `bench-score`'s `--plan-segment M01-M02-M03`: one plan-scoped attempt, `att` is a single value.
- The worker left the tree uncommitted per contract; the unit-file deletion landed in the runner's M01 commit by a staging accident (see narrative).
- Branch `bench/qwen3-8-27b` lives in workspace `.benchmarks/20260916-232231-qwen3-8-27b`; log `~/.tmp/pi-developer/20260916-232343-qwen3-8-27b.ndjson`.
