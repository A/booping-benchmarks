---
benchmark: frontmatter-update-e2e
model: qwen3.8-27b
run_id: 20260917-032036
date: 2026-09-17 03:20
outcome: pass
detail: ../runs/20260917-032036-qwen3-8-27b.md
---

# qwen3.8-27b — one agent, whole plan, thinking medium (run 4 of 6)

| harness | outcome | code | agentic | review | diff | time | tokens | cost |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| pi `direct`, one invocation for all three milestones, `--thinking medium` | pass, 1 attempt | 97.2 | 80.0 | 4.13 | 1810 | 18m54s | in 4.2M out 58.4k | $0.00 |

Every figure comes from the [run detail](../runs/20260917-032036-qwen3-8-27b.md); nothing is recomputed. Fourth run of the same-setup series started 2026-09-16 (runs 1–3) and extended 2026-09-17 (runs 4–6) for run-to-run statistics.

## The task

The fixed develop-sprint benchmark: take the groomed three-milestone plan that migrates `booping frontmatter-update`'s unit suite into a txtar e2e corpus, implement it on a throwaway clone at baseline `a025189`, and leave a branch a runner can score — corpus written and green, the superseded unit file deleted after a coverage cross-check, `just ci` green end to end.

## Sprint narrative

One `direct` invocation, one attempt, 18m54s. The worker left the tree uncommitted per contract but hand-flipped the milestone `status:` fields and checkboxes — bookkeeping the machine owns — a partial overreach between run 1's full takeover and runs 2–3's clean hands. The corpus is the best-aimed of the series: 34 cases whose coverage finally includes the three contracts every earlier run dropped — a dedicated diff-headers case, a dedicated summary-on-stderr case, and a real newline+tab round-trip delivered through a stubbed macro. M03's cross-check mapped all 42 unit functions (58 rows) and spawned two extra cases rather than writing "covered somewhere". The blemish is one faked case: `append-only-call-with-nothing-to-change-exits-1` passes no `--append` at all, duplicating nothing-to-do under a name that claims an edge it never exercises.

## Gates

All six pass: `just ci` exit 0, determinism clean, `just e2e` green (72/72), milestones terminal, changed paths inside the allowlist, unit file gone with zero stale references.

## Corpus quality

Exact-name recall 6/34, lifted to **32/34 (94.12%)** by the judge — the best mapped recall of the series; only the malformed `--append` pair and the value-with-`=` contract remain unported. Gaps 2/3 (malformed `--append` again — the one gap no run has closed). Four-channel 33/34, wildcard ratio 0.0037 (2 of 535 lines). Two branch cases map to no etalon: the faked append-only duplicate, and `logs-into-the-home-dir-vault` — genuine extra log-routing coverage born of the cross-check.

## Mutation kills

15/15 (100%) — the fourth clean sweep. The frozen set still never probes the two unported seams.

## Process profile

One plan-scoped attempt, zero malformed inputs, loops, deaths and freezes. Agentic 80.0: two blind rebaselines — `--txtar-update` runs not preceded by a red e2e — zero the rebaseline weight. Churn healthy (664 pushed vs 1810 diff). Token footprint is the series' largest: in 4.2M out 58.4k.

## Review

- **fable:medium — 4.25** (correctness 4, tests 4, code 4, scope 5)
- **codex — 4.0** (correctness 4, tests 4, code 4, scope 4)

Mean 4.13, tying run 1 for the series' best, and the finding lists are the shortest yet. Both reviewers converge on the same two: the faked append-only case (a duplicate whose title claims coverage it does not provide) and the never-ported malformed-`--append` rejection. fable adds the cross-check rows mapping the datetime/date macro units onto the `echo 1` case — coverage claims where drop rationales belong; codex adds a format violation, an explanatory paragraph where M03's contract demands heading and table only. The pattern in one sentence: the strongest corpus of the series, marred by one case that lies about what it tests.

## Standing

| run | att | code | agentic | review | diff | time | tokens |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 — 09-16 22:42 | 1 | 96.6 | 100.0 | 4.13 | 1913 | 16m17s | in 3.5M out 50.9k |
| 2 — 09-16 23:04 | 1 | 93.8 | 80.0 | 3.25 | 1539 | 14m28s | in 3.1M out 48.8k |
| 3 — 09-16 23:23 | 1 | 96.3 | 90.0 | 3.63 | 1727 | 18m24s | in 3.0M out 54.4k |
| 4 — this run | 1 | 97.2 | 80.0 | 4.13 | 1810 | 18m54s | in 4.2M out 58.4k |

Best code score of the series (and ties the model's all-time one-agent best of 2026-09-03), tied-best review, at the cost of the series' biggest token spend and two blind rebaselines. The malformed-`--append` gap is now 4-for-4 across the series — a stable blind spot of the model on this plan, not run noise.

## Footnotes

- Local provider: no OpenRouter generation ids; cost from the worker's own usage accounting.
- Scored with `bench-score`'s `--plan-segment M01-M02-M03`: one plan-scoped attempt, `att` is a single value.
- The worker left the tree uncommitted but hand-flipped milestone statuses and checkboxes (machine-owned bookkeeping); the runner validated and made the commits.
- Branch `bench/qwen3-8-27b` lives in workspace `.benchmarks/20260917-031928-qwen3-8-27b`; log `~/.tmp/pi-developer/20260917-032036-qwen3-8-27b.ndjson`.
