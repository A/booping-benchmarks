---
benchmark: frontmatter-update-e2e
model: qwen3.8-27b
run_id: 20260917-040329
date: 2026-09-17 04:03
outcome: pass
detail: ../runs/20260917-040329-qwen3-8-27b.md
---

# qwen3.8-27b — one agent, whole plan, thinking medium (run 6 of 6)

| harness | outcome | code | agentic | review | diff | time | tokens | cost |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| pi `direct`, one invocation for all three milestones, `--thinking medium` | pass, 1 attempt | 96.3 | 80.0 | 3.75 | 1695 | 18m51s | in 3.9M out 56.5k | $0.00 |

Every figure comes from the [run detail](../runs/20260917-040329-qwen3-8-27b.md); nothing is recomputed. Sixth and last run of the same-setup series (three on 2026-09-16, three on 2026-09-17) for run-to-run statistics.

## The task

The fixed develop-sprint benchmark: take the groomed three-milestone plan that migrates `booping frontmatter-update`'s unit suite into a txtar e2e corpus, implement it on a throwaway clone at baseline `a025189`, and leave a branch a runner can score — corpus written and green, the superseded unit file deleted after a coverage cross-check, `just ci` green end to end.

## Sprint narrative

One `direct` invocation, one attempt, 18m51s, contract-faithful: tree left uncommitted for the runner. 34 cases including the series' broadest scalar coverage — all eight syntax-sensitive quoting spellings, partly-numeric, value-with-`=` (its own dedicated case via the cross-check), empty-value, and the newline/tab round-trip. M03's cross-check enumerated all 58 unit rows and, finding two without homes, added cases rather than writing "covered somewhere". The model also flagged a real harness wart: `--txtar-update` clobbers hand-written `[..]` wildcards, so it restored the two log cases' wildcards by hand and re-verified clean.

## Gates

All six pass: `just ci` exit 0, determinism clean, `just e2e` green (72 cases), milestones terminal, changed paths inside the allowlist, unit file gone with zero stale references.

## Corpus quality

Exact-name recall 5/34, lifted to 30/34 by the judge. Four behaviours unported: diff mechanics as a subject (every diff sits at line 1 of a four-line frontmatter — offset hunks and body exclusion unasserted), the malformed `--append` pair, a genuinely-at-risk date-like value (the stub `2026-08-12 14:17` has no seconds and plain-loads as a string regardless, so the case named for protective quoting is vacuous), and a dedicated stream-split case. Gaps 2/3 — malformed `--append` is now 6-for-6 unported. Four-channel 33/34, wildcards clean. Four branch cases map to no etalon; two are real extra coverage (literal pass-through, home-dir vault log routing).

## Mutation kills

15/15 (100%) — the sixth consecutive clean sweep. The frozen set never probes the unported seams; the series' review findings are, collectively, a specification of where the mutation set should grow.

## Process profile

One plan-scoped attempt, zero malformed inputs, loops, deaths and freezes. Agentic 80.0: two blind rebaselines zero the rebaseline weight. Churn healthy (659 pushed vs 1695 diff).

## Review

- **fable:medium — 4.0** (correctness 4, tests 3, code 4, scope 5)
- **codex — 3.5** (correctness 3, tests 3, code 5, scope 3)

Mean 3.75. Both reviewers' worst finding is the vacuous date-like case — it passes identically whether or not the safeguard exists, and the cross-check counts it as covering two unit tests. Both name the malformed-`--append` hole, the line-1-only diff shape, the comment-free preservation fixture, and a cross-check intro paragraph its own contract forbids. One fable finding is discounted here: the unflipped DoD checkboxes under a `done` plan are the runner's bookkeeping omission in this run's close-out, not the model's — the model's contract ended at the working tree (see footnotes).

## Standing

The full six-run series, same setup end to end:

| run | att | code | agentic | review | diff | time | tokens |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 — 09-16 22:42 | 1 | 96.6 | 100.0 | 4.13 | 1913 | 16m17s | in 3.5M out 50.9k |
| 2 — 09-16 23:04 | 1 | 93.8 | 80.0 | 3.25 | 1539 | 14m28s | in 3.1M out 48.8k |
| 3 — 09-16 23:23 | 1 | 96.3 | 90.0 | 3.63 | 1727 | 18m24s | in 3.0M out 54.4k |
| 4 — 09-17 03:20 | 1 | 97.2 | 80.0 | 4.13 | 1810 | 18m54s | in 4.2M out 58.4k |
| 5 — 09-17 03:43 | 1 | 96.6 | 80.0 | 3.0 | 1647 | 17m35s | in 4.4M out 52.7k |
| 6 — this run | 1 | 96.3 | 80.0 | 3.75 | 1695 | 18m51s | in 3.9M out 56.5k |

Six-run read: code mean 96.1 with a 3.4-point spread (93.8–97.2), review mean 3.65 spanning 3.0–4.13, agentic 80–100 driven entirely by blind-rebaseline count (0/2/1/2/3/2), mutation kills 100% every time, every run pass in one attempt, wall 14m28s–18m54s. Stable signatures across all six: the malformed-`--append` rejection is never ported (6/6), the cross-check tends to certify more than the corpus holds (worst in runs 2 and 5), and blind rebaselining is habitual. Every run in the series beats the per-milestone runs of 2026-09-12/13 on score and wall clock alike.

## Footnotes

- Local provider: no OpenRouter generation ids; cost from the worker's own usage accounting.
- Scored with `bench-score`'s `--plan-segment M01-M02-M03`: one plan-scoped attempt, `att` is a single value.
- The worker left the tree uncommitted per contract; the runner's close-out for this run flipped milestone statuses but omitted the DoD checkbox flip — the review finding on unticked boxes traces there, and the branch stays exactly as scored.
- Branch `bench/qwen3-8-27b` lives in workspace `.benchmarks/20260917-040222-qwen3-8-27b`; log `~/.tmp/pi-developer/20260917-040329-qwen3-8-27b.ndjson`.
